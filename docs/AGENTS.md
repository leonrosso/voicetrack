# VoiceTrack — istruzioni per agenti AI

Questo file è l'indice per qualunque strumento AI diverso da Claude Code (che usa `CLAUDE.md`) e Cursor (che usa `.cursor/rules/voicetrack.mdc`). I tre file sono gemelli e puntano alla stessa fonte di verità.

**Leggi prima di tutto:** `docs/stato-progetto.md` (fonte unica di verità) e le voci recenti di `docs/diario-sessioni.md`.

**Regole complete** (schema Sheets, flag di deploy, frasi di test, PowerShell, segreti, aggiornamento non distruttivo della documentazione): identiche a quelle in `CLAUDE.md` — seguile alla lettera.
